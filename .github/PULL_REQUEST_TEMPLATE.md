## 概要

<!-- 何を、なぜ。1〜3 行 -->

## 変更

<!-- 箇条書き。設計判断があれば「なぜそうしたか」を添える -->

## Issue

<!--
WBS 番号つきの PR は必須（PreToolUse ガードが本文を見て deny する）。
  Closes #N          … マージ時に GitHub が自動で閉じる（推奨）
  Refs #N            … この PR では閉じない（WBS 項目が複数 PR にまたがる場合）
  Issue: なし（理由） … 対応する Issue が無い場合
-->

## 検証（DoD — CLAUDE.md §4b）

- [ ] **画面テスト** — 分岐・サービスへ渡す引数・遷移先（`@testing-library/react-native`）
- [ ] **実機確認** — エミュレータ/実機で該当導線を通し、スクリーンショットを添付
- [ ] **個人情報の非公開確認** — 資産の追加/差し替え・ストア掲載物を触った場合のみ
      （`release-verify` §6。`assets/` に置いたものは配布される）

<!--
画面テストは実機確認の代わりにならない。react-test-renderer では再現しない不具合の実績:
PressableScale の flex 潰れ / borderRadius が効かず四角になる /
ボトムシートがジェスチャーバーに飲まれる / 横スクロール帯が縦にも伸びる。
該当しない項目は「N/A（理由）」と書いて消さない。
-->
