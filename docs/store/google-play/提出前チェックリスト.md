# Play 提出前チェックリスト

> 作成: 2026-08-11（WBS 3.8）
> **アカウント側の作業はコードでは埋められない。** ここに「あなたがやること」と
> 「こちらで済んでいること」を分けて書く。

## A. あなたにしかできない作業

### A-1. AdMob（WBS 3.7 の残り）

現状は **Google 公式のテスト ID** が入っていて、テスト広告が出る状態。実 ID に差し替える。

1. AdMob アカウントを作る（Google アカウントで登録）
2. アプリを登録する（Android / パッケージ名 `com.saientecho.app`）
3. 次の 2 つを発行して控える
   - **アプリ ID**（`ca-app-pub-XXXXXXXX~YYYYYYYY` の形）
   - **アプリ起動（App Open）広告ユニット ID**（`ca-app-pub-XXXXXXXX/ZZZZZZZZ` の形）
4. こちらに渡す → `app.json` と env に入れて焼き直す

```
app.json  → plugins > react-native-google-mobile-ads > androidAppId
ビルド時  → EXPO_PUBLIC_ADMOB_APP_OPEN_UNIT_ID=ca-app-pub-.../...
            EXPO_PUBLIC_ADMOB_ENABLED=true
```

> **実 ID を入れたビルドで自分の広告を何度もタップしないこと。** 無効なトラフィックとして
> アカウントが停止されることがある。動作確認はテスト ID のままで済ませる。

### A-2. プライバシーポリシーの公開 URL

Play Console は **URL** を要求する。原稿は `docs/プライバシーポリシー.md` にある。

- 独自ドメインは**取らない決定**（インフラ設計 D2）なので、GitHub Pages か Gist が候補
- 公開したら URL をこちらに渡す（掲載情報にも載せる）

### A-3. Play Console での入力

- **データセーフティ申告** — `docs/store/google-play/data-safety.md` を見ながら入力
- **掲載情報** — `docs/store/google-play/listing-ja.md` が正。
  サービスアカウント JSON（既定 `C:/secure/play-service-account.json`）を置けば
  `node scripts/release/update-play-listing.mjs` で流し込める
- **コンテンツのレーティング**アンケート
- **対象年齢・広告の有無**の申告（**広告あり**）
- **クローズドテスト**の設定（判断④: 個人アカウントはテスター数 × 14 日の要件がありうる）

### A-4. 署名

現状のリリースビルドは**デバッグ鍵で署名**されている（`signingConfig signingConfigs.debug`）。
サイドロード用には問題ないが、**Play 提出には本番鍵が要る**。

- Play App Signing を使う前提で、アップロード鍵を作る
- 鍵とパスワードの保管場所を決める（**紛失すると更新できなくなる**）

## B. こちらで済んでいること

|                          | 状態                                                                                      |
| ------------------------ | ----------------------------------------------------------------------------------------- |
| 起動広告（頻度制限つき） | 実装・テスト・実機確認済み（テスト広告が出ることを AQUOS で確認）                         |
| UMP 同意導線             | 実装済み。地域判定は SDK に任せる。設定の「広告のプライバシー設定」行は要る地域でのみ表示 |
| AD_ID                    | ブロック解除済み（パーソナライズ広告・判断⑧）                                             |
| 権限の整理               | BILLING / RECORD_AUDIO / SYSTEM_ALERT_WINDOW を除去。aapt で APK を直接確認済み           |
| 掲載文                   | `listing-ja.md`（短い説明 44/80 字・詳しい説明 1356/4000 字）                             |
| プライバシーポリシー     | 原稿あり（**公開 URL が未定** → A-2）                                                     |
| データセーフティ         | 下書きあり（**入力は手動** → A-3）                                                        |
| スクリーンショット       | 取得スクリプトをさいえん手帳の画面に差し替え済み                                          |
| アイコン                 | `mark.svg` から生成済み（`pnpm assets:brand`）                                            |

## C. 初回提出（2026-08-12）で完了

A・B の全項目と、当時「まだ手つかず」だった次を消化した。実務の手順は
`docs/リリース手順.md` に移したので、**2 回目以降はそちらを見る**。

- フィーチャーグラフィック → `scripts/release/generate-feature-graphic.mjs`（若葉パレットで新規作成）
- AAB のビルドと検証・アップロード鍵の作成 → リリース手順 §5
- 3.10 / 3.11 の AI → **BYOK ではなくサーバー経由で実装**（決定⑨）。
  データセーフティ §3 もそれに合わせて書き換え済み

## D. 提出前に必ず通すゲート

### D-1. 出す順番

**データセーフティ申告は AI のスコープが決まってから書く。**
写真が外部へ出るかどうかで申告内容が変わる。先に申告して後から機能を足すと、
実装と申告の不一致になる（ポリシー違反になりうる）。

### D-2. 個人情報が配布物・ストアから出ないか（DoD 3）

**手順は `release-verify` Skill §6。** 以下を機械的に確認してから提出する。

- [ ] 提出する AAB を展開し、**同梱されている画像・音声を実物で列挙**した
      （`assets/` に置いたものは配布される。`EXPO_PUBLIC_ENABLE_SAMPLE_DATA` のような
      実行時フラグでは同梱を止められない — Metro が `require()` をビルド時に静的解決するため）
- [ ] 同梱写真の **EXIF が 0・GPS が 0**、かつ生バイト列に GPS/端末名の痕跡が無い
      （スマホの写真は既定で自宅の座標を持つ）
- [ ] スクリーンショットに実名・住所・電話番号・メールの**写り込み**が無い
- [ ] 掲載文・リリースノートに開発者の個人情報が混ざっていない
- [ ] 課金を入れる場合、**氏名+住所が Play に公開される**ことを承知している
      （無料+広告のみなら公開されるのはメールのみ。v1.5 の 4.3 着手時の判断ポイント）
