# 検討: 作物マスターを 30 品目から 100 品目へ（2026-09-05）

> 発端: 「小松菜は収穫まで何日と表示されたが、ルッコラと空芯菜は表示されなかった」。
> 原因は不具合ではなく、作物マスター（`apps/mobile/src/db/crop-master.ts`・v3）に
> 2 つとも載っていないため。ここから「100 品目くらい行きたい」となった。
> **実装はまだしていない。** 設計判断とデータの段取りを決めた記録。第 1 段の Issue は #180。
>
> 関連: R08（栽培暦）/ R09（作物ガイド）/ WBS 3.1（30 作物・判断②）/ WBS 4.12（旬への細分化）/
> docs/栽培暦の出典.md（出典と検証記録）

---

## 0. 結論（3 行）

- **100 品目は出せる。律速はコードではなくデータ。** 1 品目あたり出典の照合込みで 45〜75 分、
  70 品目で 50〜90 時間。コード側の改修は 1〜2 日
- **列は第 1 段で決め切る**（§3-1 の 9 項目）。あとから足すと 100 品目を触り直す。
  収穫の幅がそろえば「そろそろ収穫」のローカル通知を天気（4.13）と合わせて出せる（§8）
- **データを足す前に設計を 3 か所直す。** 「今月の菜園仕事」の件数上限・作物ガイド一覧の分類と検索・
  多年草（収穫日数が単一の数値で表せない）
- **3 段階で入れる。** 50 → 75 → 100。各段で `CROP_MASTER_VERSION` を上げる

---

## 1. いま何が起きているか

| 事実                                                                                     | 場所                                           |
| ---------------------------------------------------------------------------------------- | ---------------------------------------------- |
| マスターは 30 品目（秋冬 12 + 春夏 18）                                                  | `crop-master.ts` `CROP_MASTER`                 |
| 「収穫まで N 日」はマスターの `harvestAfterDays` から計算。無ければ帯を描かない          | `growth-progress.service.ts:79-89`             |
| 出典は**全体で 1 リスト**（4 資料）。作物ごとの対応は持っていない                        | `crop-master.ts` `CROP_MASTER_REFERENCES`      |
| 寒冷地の実期の裏どりは JA 北海道の **13 品目だけ**。中間地・暖地は「定説の範囲」で未補正 | docs/栽培暦の出典.md                           |
| 別名表がマスターに**無い**作物を指している（トウガラシ・インゲン・サヤエンドウ）         | `crop-match.service.ts` `CROP_NAME_ALIASES`    |
| 「今月の菜園仕事」は該当作物名を読点でつないで**全部**出す。上限なし                     | `MonthlyWorkCard.tsx:59`                       |
| 作物ガイド一覧は科を添えた単純な一覧。検索・分類なし                                     | `app/(tabs)/crops/index.tsx`                   |
| DB の `crop_guides.harvest_after_days` は **nullable**。「次の作業」も null を素通しする | `schema.ts:143` / `next-action.service.ts:130` |

最後の行が効いていて、多年草を入れるのに DB 移行は要らない。マスターの型を緩めるだけでよい。

---

## 2. 決めたこと（2026-09-05）

| #   | 論点                                             | 決定                                                                                                                                                               |
| --- | ------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| ①   | 多年草（アスパラガス・ミョウガ・果樹）を入れるか | **入れる。** `harvestAfterDays: number \| null` にし、`perennial: true` を持たせて「翌年から」と出す                                                               |
| ②   | 出典を作物ごとに持つか                           | **持つ。** マスターに `sourceIds: string[]` を足し、`CROP_MASTER_REFERENCES` に `id` を付ける。DB 列は足さない（ガイド画面がコードのマスターを id で引けば足りる） |
| ③   | 公的資料で足りない分に種苗会社の公開表を使うか   | **公的を優先し、不足分だけ許す。** ハーブ・果樹は県指針に薄いので、農研機構・県の園芸試験場 → それでも無ければ種苗会社の公開表。出典として明記する                 |

### ①の理由

「植え付けから収穫まで何日」は一年草の枠組みで、多年草は「植えて翌年（果樹は 2〜3 年）から毎年採る」。
無理に日数を置くと進行帯が嘘になる。null なら進行帯は既に「目安なし」として経過日数だけを出す設計
（`growth-progress.service.ts` 冒頭のコメント）で、「次の作業」も null を提案対象から外す。
壊れるのはテスト `ガイドの日数は 追肥 < 収穫` だけで、null を除外すればよい。

### ②の理由

100 品目で全体 1 リストだと「この作物はどの資料か」が追えない。資料を差し替えたときの影響範囲も
分からなくなる。作物ガイド画面の脚注に「この作物の出典」を出せるようになる。

### ③の理由

docs/栽培暦の出典.md の方針「人手レビューの代わりに公的資料と突き合わせる」を崩さない。
ただし公的資料の作型表は主要野菜に偏っていて、ハーブ・果樹・地方野菜は載らないことが多い。

---

## 3. 設計変更（データより先に入れる）

### 3-1. マスターの型

```ts
export interface CropMaster {
  id: string;
  name: string;
  nameReading: string;
  family: string;
  /** 一覧の分類・検索の絞り込み。DB の crops.category にも持つ */
  category: CropCategory;
  defaultUnit: ...;
  /** 多年草。収穫日数を持たず、ガイドに「翌年から収穫」と出す */
  perennial?: true;
  /** CROP_MASTER_REFERENCES の id。1 つ以上 */
  sourceIds: string[];
  calendars: CropCalendarWindow[];
  guide: CropGuideMaster;
  /** 出典を伴わない編集者の判断。出典つきの guide と分けて持つ */
  editorial: CropEditorial;
}

export type CropCategory =
  | 'leaf'    // 葉菜
  | 'root'    // 根菜
  | 'fruit'   // 果菜
  | 'bean'    // 豆
  | 'tuber'   // いも
  | 'allium'  // ネギ類
  | 'herb'    // ハーブ
  | 'tree';   // 果樹・多年

/** 出典と突き合わせる値。県指針・種袋の裏に載る範囲だけ */
export interface CropGuideMaster {
  spacingCm: number;
  sunlight: 'full' | 'partial';
  wateringNote: string;
  /** 水やりの目安間隔（日）。R26 ケアスケジュール自動提案の既定値 */
  wateringIntervalDays: number | null;
  /** 発芽までの日数。「まだ芽が出ない」に「まだ普通」と言うため */
  germinationDays: number | null;
  /** 種まきから定植までの日数（苗物だけ）。R33 定植適期の材料 */
  transplantAfterDays: number | null;
  /** 初回の追肥（既存） */
  fertilizeAfterDays: number | null;
  /** 2 回目以降の追肥間隔（日）。果菜の「2〜3 週おき」を「次の作業」に出す */
  fertilizeIntervalDays: number | null;
  /** 収穫の目安（既存・幅の中央）。多年草は null */
  harvestAfterDays: number | null;
  /** 収穫の幅。進行帯に「窓」を描く（growth-progress が「幅を出せるデータが無い」としていた欠落） */
  harvestWindowDays: { min: number; max: number } | null;
  /** 初収穫から採り続けられる日数。「片付けどき」を出す */
  harvestDurationDays: number | null;
  /** 発芽適温・生育適温（℃）。WBS 4.13 の天気連携と R33 が使う */
  temperature: { germination: [number, number]; growth: [number, number] } | null;
  /** 連作を避ける年数。R17 は科だけでは足りない（ナス科 3〜4 / ウリ科 2〜3 / 豆 1〜2） */
  rotationYears: number | null;
  /** 作物ごとの作業（摘芯・支柱・土寄せ・芽かき・摘果）。「次の作業」を収穫と追肥の 2 種類から広げる */
  tasks: { kind: CropTaskKind; afterDays: number; note?: string }[];
  commonPests: string[];
  tips: string;
}

export type CropTaskKind =
  | 'pinch'      // 摘芯
  | 'stake'      // 支柱
  | 'hill'       // 土寄せ
  | 'thin'       // 間引き
  | 'sucker'     // 芽かき
  | 'fruit-thin' // 摘果
  | 'net';       // 防虫ネット

/** 出典なしの判断。一覧の絞り込みと登録時の助言にだけ使い、暦や次の作業の判定には使わない */
export interface CropEditorial {
  /** 初心者向けか */
  beginner: boolean;
  /** プランターで育つか。育つなら必要な深さ（cm） */
  container: { ok: true; depthCm: number } | { ok: false };
}
```

**列は第 1 段で決め切る。** 70 品目を書いてから足すと全部触り直しになる。
上の 9 項目（水やり間隔・発芽日数・定植日数・追肥間隔・収穫の幅・収穫の続く期間・適温・
連作年数・作業）はどれも県指針や種袋の裏に載る値で、出典の照合コストがほぼ増えない。
既存 30 品目にも同じ列を埋め直す（v4 で一緒に配る）。

**入れないと決めたもの:** 収量の目安（品種と環境でぶれすぎ、統計と比べると落胆を生む）・
病害虫の発生月（出典が地域で割れ、月単位の暦の外に矛盾を作る）・
コンパニオンプランツ（定説の域で出典が薄い。入れるなら注意書きの文章として tips に）。

- `category` は **`crops` テーブルに列を足す**（一覧・検索が DB から引くため）。移行 1 本と
  `syncCropMaster` の書き込み追加。既存のユーザー作成作物は null のまま
- `sourceIds` は DB に持たない（②）
- `CROP_MASTER_REFERENCES` の各要素に `id`（例: `maff-sehi`, `ja-hokkaido`）を付け、
  構造テストで「全作物の sourceIds が実在する id を指す」を強制する

### 3-2. 「今月の菜園仕事」の畳み込み

行ごとに **6 種まで**出し、残りは「ほか N 種」。タップで作物ガイドへ、**今月で絞った状態**で開く。
30 品目でも 5 月のまきどきは 10 種を超えているので、これは 100 品目の前から要る。

### 3-3. 作物ガイド一覧

分類（3-1 の `category`）でセクションを切り、上に検索欄（名前・読み・別名）。
「今月まける／植えられる／採れる」のフィルタチップを 1 つ持つと 3-2 の受け皿になる。

### 3-4. 小さいもの

- **id の命名**: 既存はローマ字（`crop-kyabetsu`）と英語（`crop-cucumber`）が混在。
  **既存は変えない**（`plantings.crop_id` に保存済み。変えると栽培が孤児になる）。
  新規は **和名のヘボン式ローマ字**で統一（`crop-rukkora` / `crop-kushinsai` / `crop-shishito`）
- **別名表の空振り解消**: トウガラシ・インゲン・サヤエンドウを第 1 段で入れる
- **テスト `30 作物そろっている`** は「第 N 段の目標数」に書き換える（下限で見る）
- **AI の写真登録**（1.3）は作物名で名寄せしているので、マスターが増えるほど当たる。
  追加作業は要らないが、別名表（店頭の呼び方）は品目と一緒に足す

---

## 4. 出典の増やし方

| 地域帯 | いまの資料                 | 足す候補                                                                            |
| ------ | -------------------------- | ----------------------------------------------------------------------------------- |
| 寒冷地 | JA 北海道（13 品目）       | 青森・長野の県指針（農水省収載）。葉物・豆・ハーブの実期                            |
| 中間地 | 農水省収載ページ（未補正） | 千葉・茨城・埼玉の県指針から 1〜2 件。**作型図がある県**を選ぶ                      |
| 暖地   | 長崎（ジャガイモのみ）     | 熊本・鹿児島 + **沖縄**（空芯菜・ショウガ・ゴーヤ・ヘチマ）                         |
| 横断   | —                          | 農研機構の野菜情報・県の園芸試験場（ハーブ・果樹）。不足分のみ種苗会社の公開表（③） |

4 件 → **12〜15 件**の見込み。各資料に `id` を付け、作物側から `sourceIds` で参照する。
資料は docs/栽培暦の出典.md の表に追記し、補正の記録も同じ文書に残す（既存の手順どおり）。

---

## 5. 100 品目の候補と段階

既存 30 は変えない。新規 70 を 3 段に分ける。**段ごとに `CROP_MASTER_VERSION` を上げる。**

### 第 1 段: +20 → 50 品目（設計変更と同時）

穴として現れているものと、家庭菜園の頻出。多年草は 2 つだけ入れて型を通す。

| 分類   | 品目                                                               |
| ------ | ------------------------------------------------------------------ |
| 葉菜   | ルッコラ・空芯菜・チンゲンサイ・モロヘイヤ・サンチュ・カリフラワー |
| 根菜   | ラディッシュ・ショウガ                                             |
| 果菜   | トウガラシ（シシトウ含む）・パプリカ・スイカ                       |
| 豆     | インゲン・サヤエンドウ・ソラマメ・ラッカセイ                       |
| いも   | サトイモ                                                           |
| ネギ類 | 長ネギ・ニラ（多年）                                               |
| ハーブ | パセリ                                                             |
| 多年   | ミョウガ（多年）                                                   |

### 第 2 段: +25 → 75 品目

| 分類   | 品目                                                       |
| ------ | ---------------------------------------------------------- |
| 葉菜   | ケール・ターサイ・ノザワナ・タカナ・ミツバ・セロリ・エゴマ |
| 根菜   | ビーツ・ゴボウ・ナガイモ                                   |
| 果菜   | メロン・マクワウリ・トウガン                               |
| 豆     | ササゲ・ツルムラサキ                                       |
| いも   | ヤーコン                                                   |
| ネギ類 | ラッキョウ・ワケギ・アサツキ                               |
| ハーブ | ミント・ローズマリー・タイム・コリアンダー・ディル         |
| 多年   | アスパラガス                                               |

（25 品目。ツルムラサキは分類上は葉菜だが、つる性で豆と同じ支柱の作業になるので豆の段で書く）

### 第 3 段: +25 → 100 品目

| 分類   | 品目                                                                               |
| ------ | ---------------------------------------------------------------------------------- |
| 葉菜   | カラシナ・ワサビナ・スイスチャード・コールラビ・芽キャベツ・エンダイブ・セリ       |
| 果菜   | ヘチマ・ハヤトウリ・シカクマメ                                                     |
| ハーブ | カモミール・レモングラス・オレガノ・セージ・イタリアンパセリ・チャイブ・ラベンダー |
| 多年   | フキ・ルバーブ                                                                     |
| 果樹   | ブルーベリー・イチジク・ラズベリー・ユズ・レモン・ブドウ                           |

### 落としたもの

- 穀物・油糧（ソバ・ゴマ）: 家庭菜園の手帳の主語ではない
- 観賞用（ヒマワリ・ナスタチウム）: 「収穫」の枠に合わない
- 品種レベル（ミニトマト・ミニ白菜・九条ねぎ）: **別名表で親に寄せる**。マスターに増やさない

---

## 6. 1 品目あたりの執筆項目（チェックリスト）

- [ ] id（ヘボン式）・名前・読み・科・分類・既定単位
- [ ] 暦の窓: 3 地域帯 × （まき or 植え）+ 収穫 = 最低 6 窓。苗物は植えも。年またぎは start > end
- [ ] ガイド: 株間・日照・水やり（文と間隔）・発芽日数・定植日数（苗物）・追肥（初回と間隔）・
      収穫（目安・幅・続く期間。多年草は null）・適温・連作年数・作業（摘芯/支柱/土寄せ…）・虫と病気・コツ
- [ ] 編集者の判断: 初心者向けか・プランター可否と深さ（出典なし。暦や判定には使わない）
- [ ] `sourceIds`（1 つ以上・実在する id）
- [ ] 別名（店頭・種袋の呼び方だけ。学名・地方名は入れない）
- [ ] docs/栽培暦の出典.md に、資料と食い違って補正した点があれば追記

---

## 7. WBS 4.12（旬への細分化）との順番

4.12 を先にやると、新規 70 品目を最初から旬で書くことになり、旬の作型表が無い品目で詰まる。
**4.19（本件）を月単位で先に入れ、4.12 は「月までしか分からない品目は上旬〜下旬まるごと」を
許す形にする**。そうすれば 100 品目の書き直しは強制されない。

---

## 8. 通知への展開 — 「そろそろ収穫」を天気と合わせて出す

上の列がそろうと、**ローカル通知**で「そろそろ収穫」を出せる。

- **材料**: `harvestWindowDays`（収穫の幅）で「窓に入った日」が決まる。
  今の単一日数だと「目安日を過ぎた」しか言えない
- **載せ方**: 既存の `reminder.service.ts` と同じ（次の 1 回だけを日時指定で予約し、起動時に積み直す）。
  サーバーは使わない。新しい仕組みは要らない
- **天気との合わせ方**: WBS 4.13（#136）が「毎日判定して水やりを間引く」層を作る。
  同じ判定の中で「収穫の窓に入っていて、晴れが 2 日続く」なら「そろそろ収穫。週末が採りどき」と
  1 本にまとめる。**水やり通知と別立てで鳴らさない**（#136 の「二重に鳴らさない」を守る）
- **順番**: 4.19 第 1 段（幅のデータ）→ 4.13（天気の判定層）→ 通知の文面。
  幅のデータが無いまま 4.13 に入ると「そろそろ」を言えないので、**4.19 が先**
- **言い方**: 「収穫できます」と言い切らない。窓は目安で、実際は株を見て決める（利用規約の免責）

## 9. 見積もり

| 工程                                              | 規模        |
| ------------------------------------------------- | ----------- |
| 設計変更 3-1〜3-4（コード・テスト込み）           | 1〜2 日     |
| 出典の収集と id 付け                              | 半日        |
| 第 1 段 20 品目のデータ                           | 10〜20 時間 |
| 第 2 段 25 品目                                   | 12〜25 時間 |
| 第 3 段 25 品目（ハーブ・果樹は資料が薄く上振れ） | 15〜25 時間 |

配布はアプリ更新のみ。サーバー側の作業は無い。
